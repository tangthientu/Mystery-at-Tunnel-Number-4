using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GetScript : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject scriptfrom;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
